<%= simple_form_for([@task, @task.steps.build],html: {class: "form-horizontal"}) do |f| %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :image_url %>
  <%= f.input :file_url %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
